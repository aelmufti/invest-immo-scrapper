# Web / API
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.2

# Database
SQLAlchemy>=2.0
alembic>=1.13

# Scheduler
APScheduler>=3.10

# HTTP / data
httpx>=0.27
requests>=2.31
pandas>=2.2
numpy>=1.26
python-dotenv>=1.0

# Geo / cartographie
folium>=0.16
streamlit-folium>=0.20

# UI
streamlit>=1.32
plotly>=5.20

# Notifications (desktop optionnel — plyer marche partout, fallback si absent).
# Sur macOS, plyer a besoin de pyobjus pour les notifications natives.
plyer>=2.1; sys_platform != "linux"
pyobjus>=1.1; sys_platform == "darwin"

# Export
openpyxl>=3.1

# Tests
pytest>=8.0
pytest-asyncio>=0.23

# Parsing / scraping (couche B optionnelle)
beautifulsoup4>=4.12
lxml>=5.1
